flask>=3.0
fastapi>=0.111
uvicorn[standard]>=0.30
numpy>=1.26
torch>=2.2
# Optional — only needed for the chat panel in demo_app.py:
openai>=1.0          # OpenAI provider (default)
google-genai>=0.3    # Gemini provider (choose Gemini in the top-right key box)
# Optional — DKA real-data extraction/calibration utilities:
pandas>=2.0
scikit-learn>=1.4
scipy>=1.12
joblib>=1.3
duckdb>=1.0
# Optional — MIMIC-IV Waveform candidate feature extraction:
wfdb>=4.3
